# 기업마당 지원사업 추천

기업마당의 공개 지원사업 목록을 기존 공공기관 뉴스 수집기에 추가한다. 홈 세 번째 카드와 `/agency-news.html`에서 새 지원사업 수, 공고 원문, 신청기간, 추천 이유, 확인할 조건을 제공한다. 별도 메시지·메일은 발송하지 않는다.

## 회사 조건과 선별

기본 회사 정보는 `data/company_support_profile.json`에 있다. 주식회사 콤파스원, 더비다 요양원, 2024년 설립, 여성 대표, 성남 본사, 안양 연구소, 인천·안양 요양원 운영, SaaS 개발을 기준으로 한다. 추가 정보는 Railway의 `BUSINESS_SUPPORT_PROFILE_JSON`으로 덮어쓴다. 직원 수·매출 등 실제 값은 이 서버 설정에만 두고 Git이나 공개 API 응답에 포함하지 않는다. `certifications`는 기본값과 병합하며 확인되지 않은 인증은 null로 둔다.

선별 코드는 `business_support.py`다. 제목뿐 아니라 기업마당 요약의 지원 대상과 지원 내용을 확인한다. SaaS·소프트웨어, 돌봄, 창업, 여성기업, 연구개발, 지식재산, 인력과 기업 운영 지원을 후보로 삼는다. 명확한 다른 지역·시군 제한, 제조업 등 다른 업종, 예비창업자 전용, 설립·고용 규모와 명백히 어긋나는 조건은 제외한다. 여성 대표자와 여성기업확인서를 구분하며, 지역 요건은 본사·연구소·사업장 중 어느 소재지를 인정하는지 확인 대상으로 표시한다.

후보는 **신청 가능 판정이 아니다.** 원문 첨부파일의 세부 기술 수요, 법정 중소기업·소상공인 판정, 기준연도별 매출, 제외 업종, 중복수혜·자부담 등은 자동 확정하지 않는다. 기술 확장이나 추가 인증이 필요한 사업은 조건을 먼저 검토하도록 안내한다. 요양원 수가 매출과 SaaS 개발 사업의 인정 범위도 확인 조건으로 남긴다. 모든 가능성을 판정하는 법률·자격 심사기는 아니다.

## 수집과 알림

- 기존 `AGENCY_NEWS_SYNC_ENABLED`와 `/data/agency-news.db`를 사용한다. 매일 09:10 KST, 최초 실행 또는 놓친 일정은 즉시 수집한다.
- 최초에는 기업마당의 진행 중 공고를 등록일 내림차순으로 최근 10페이지(150개) 확인한다. 과거 전체 공고를 소급하지 않는다.
- 이후 최소 2페이지를 겹쳐 읽고, 한 페이지 전체가 이전에 확인한 공고가 될 때까지 탐색한다. 최대 100페이지에 도달하면 성공으로 기록하지 않는다.
- 공고 ID로 중복을 막는다. 제목·게시일·신청기간 변경 시 재검토하고, 추천에서 제외된 기존 공고는 보관하되 활성 추천에서 뺀다. `first_seen_at`은 수정·재배포 후에도 유지한다.
- 목록/상세 요청 실패 시 기업마당의 이전 성공 이력과 추천을 유지하고 30분 후 재시도한다. 다른 공공기관 수집은 독립적으로 진행한다.
- 명시된 신청 종료일이 지난 공고는 기본 화면과 새 공고 수에서 제외한다. ‘상시 접수’, ‘예산 소진 시’ 등은 현재 접수를 보증하지 않고 ‘접수 여부 확인’으로 표시한다. 접수 예정 공고는 별도로 표시한다.
- 새 공고 확인 기록은 브라우저 `localStorage`의 `vida-support-read-v1`에 ID로 저장한다. 사용자가 ‘지원사업 모두 확인’을 눌러야 읽음 처리한다. 새로고침·다른 탭에서도 기록을 유지하며, 다른 브라우저와는 공유하지 않는다.
- 공개 API의 `support`는 전체 추천 수, 기간이 지나지 않은 후보 수, 새 공고 표시를 위한 최소 ID 정보를 포함한다. 회사의 매출·고용 설정은 반환하지 않는다.

## 관심 선택과 추천 순위

- 지원사업 카드의 `관심있음` / `관심없음`은 회사 공용 선택이다. 같은 버튼을 다시 누르면 취소되며, 다른 버튼을 누르면 기존 선택을 교체한다. 개인별 투표 누적 기능은 아니다.
- `POST /api/agency-news/support-preference`는 `{ "article_id": "bizinfo:PBLN_…", "preference": "interested|not_interested|neutral", "reason": "관심없는 이유" }`을 받는다. 새 화면은 관심없음에 1~500자 이유를 필수로 보내고, 관심있음·취소에는 빈 문자열을 보낸다. 기존 지원사업만 허용하고 JSON 8KB·값·출처를 검사한다. 이미 열린 구버전 화면의 `reason` 생략 요청은 호환하며 기존 이유를 지우지 않는다. 개선요청 웹훅이나 메시지는 보내지 않는다.
- 선택은 기존 `/data/agency-news.db`의 별도 `support_preferences` 테이블에 공고당 한 행으로 저장한다. 일일 수집·공고 수정·재배포로 지워지지 않는다. 별도 개인정보나 브라우저 식별자는 수집하지 않는다.
- 조회할 때 관심 사업을 우선 배치한다. 관심있음과 이유가 없는 기존 관심없음은 각 선택의 +1/-1을 해당 공고의 분야 수로 나누어 분야별 가중치를 합산한다. 후보와 겹치는 분야의 합 × 12를 기존 회사 조건 점수에 더하되 조정폭은 ±40으로 제한한다. 신규 후보에도 적용하며 반복 클릭으로 가중치가 누적되지 않는다.
- 관심없음 사업은 기본 목록·홈 새 공고 수에서 제외한다. `지원사업 관심 → 관심없음` 또는 `전체 · 관심없음 포함`에서 다시 볼 수 있다. 관심 취소 시 그 선택의 분야 가중치도 제거한다.
- 공공기관 뉴스 순서는 유지한다. 지역·업종 등 기존 선별 조건과 마감·추천 제외 판단은 관심 선택으로 완화하지 않는다. 실패하면 저장 확인 또는 목록 갱신 실패를 표시하고 다시 시도할 수 있다.

## 관심없는 이유와 다음 추천

- 관심없음 버튼은 이유 입력창을 연다. 입력 예시를 선택하거나 자유롭게 작성할 수 있으며, 저장 전 취소는 아무것도 변경하지 않는다. 기존 관심없음 목록에서도 이유를 추가·수정할 수 있다. 입력창을 연 동안 자동 갱신이 입력을 덮어쓰지 않으며 저장 실패 시 글을 유지한다.
- `support_preferences.reason` 열을 자동으로 추가해 기존 선택을 보존한다. 이유는 공고 수집과 분리되어 재수집·재배포에도 남는다. 관심있음으로 바꾸거나 관심을 취소하면 해당 이유의 추천 영향도 제거한다.
- `support_feedback.py`는 명시된 지원유형·조건을 제한된 규칙으로 해석한다. 외부 AI 호출이나 모델 학습은 하지 않는다. 입주·공간, 교육·행사, 투자·IR, 수출, 특허, 인력, 융자, 제조, 로봇, 도입·상용화, 연구·시니어·AI·실증, 자부담을 구분한다. 원하는 분야를 거절 대상으로 삼지 않도록 긍정 표현과 대조 문장을 구분한다. `연구과제만`, `시니어와 관련 없는 사업` 등은 해당 관련성이 확인되지 않은 후보를 덜 추천한다.
- **이유를 입력한 관심없음은 기존의 광범위한 분야 감점을 대체한다.** 확인된 조건과 후보의 제목·지원대상·지원내용을 비교해 이유 한 건당 -24, 전체 이유 감점은 -60까지 적용한다. 공통 확인사항에 있는 `자부담 확인` 문구는 실제 자부담의 근거로 쓰지 않는다. 다른 후보를 자동 탈락시키거나 신청 자격을 바꾸지 않는다.
- 이번 일정·시기 문제처럼 일시적인 이유나 구체적 조건을 확인하기 어려운 문장은 해당 사업에만 적용한다. 원문 이유와 실제 반영 기준을 관심없음 카드에 표시하고, 영향을 받은 다른 후보에도 순위를 낮춘 조건을 설명한다. 임의 문장의 의미·금액 임계값·세부 자격을 모두 이해한다고 가정하지 않는다.

## 검증

2026-09-10부터 서강대학교 시니어케어 AX센터를 **공동연구 협력기관**으로 기록한다(사용자 확인). 안양 기업부설연구소와 별도이며 서울 소재 회사로 판정하지 않는다. `prioritize_senior_ai_research`가 켜져 있으면 제목·대상·지원 내용에 시니어 케어와 AI·AX 연구·실증이 함께 명시된 후보에 +60, 시니어 AI 상용화 후보에 +40, AI 또는 시니어 분야의 대학·연구기관 협력 연구에 +25를 조회 시 더한다. 기존 관심 선택은 우선하고, 마감·제외 조건은 유지한다. 교육생·인턴십·경진대회·무관한 기술 분야는 연구 우선 추천에서 제외한다. 공고에 정해진 대학 산학협력단·연구책임자·기업 참여 자격은 별도 확인한다.

현재 자동 수집 출처는 기존 공공기관 게시판과 기업마당이며 IRIS·한국보건산업진흥원의 모든 연구공고를 직접 수집하는 것은 아니다. 선별 정책 버전을 올려 최신 150개 공고와 기존 보관 후보를 재검토한다.

`python -m unittest test_support_feedback test_support_preferences test_business_support test_agency_news` 및 `node --test test_agency_news.cjs`로 이유 해석·기존 DB 전환·저장·수정·취소·반복 요청·재수집·순위 변동·새 후보 반영·알림 제외·원문 자격 조건 유지·API 오류·목록 필터를 검증한다.

`python -m unittest test_business_support test_agency_news`와 `node --test test_agency_news.cjs`는 실제 게시판 형태의 목록·상세 파싱, 지역/업종/업력/인력 필터, 여성기업 확인 조건, 접수기간, 페이지 탐색, 변경·중복·오류 시 보존, API 접근 제한, 새 공고 및 마감 필터를 검증한다.

회사 조건이나 선별 규칙을 바꾸면 기존의 모든 과거 비추천 공고를 자동으로 재심사하지는 않는다. 당시 기준으로 새로 확인되는 공고에 적용한다. 화면과 수집 로그를 함께 확인해 게시판 구조가 바뀌면 파서를 갱신한다.
