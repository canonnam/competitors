# 지원사업 준비·기록

홈의 지원사업 카드에서 `/support-projects.html`로 진입한다. 별도 담당자 접근 키 없이 열리며 기본 정보/초안 기능은 `/support-prep.html`에 유지한다.

- 첫 과제는 `2026-지역AX-2 · 1-1-2`. 공고·RFP·사용자 엑셀을 대조한 핵심 조건, 요구사항 10개, KPI 13개, 준비 항목 10개, 컨소시엄 역할 제안을 등록한다. 대표님 Claude 페이지는 확정 사실과 구분한 참고 링크다.
- 공고의 접수 마감(2026-09-30 15:00 KST), 기업 주관의 연구소 요건, 전남광주 총 사업비 50% 이상 참여 및 지역 실증 필수 조건을 반영한다.
- `data/ax-2026-project.json`은 첫 실행에만 `INSERT OR IGNORE`된다. 재배포가 사용자가 수정한 내용을 덮어쓰지 않는다.
- 준비 항목·기관 역할·메모·예산안은 변경사항 저장으로 저장한다. 기록 추가와 공유 링크 생성은 저장을 포함한다. 프로젝트 목록으로 이동할 때도 미저장 변경을 먼저 저장하며, 브라우저를 닫을 때는 미저장 경고를 표시한다.
- 기존 `/data/support-applications.db`의 별도 `grant_*` 테이블에 영구 저장한다. 전체 프로젝트 변경 이전 값은 `grant_history`에 보관하며 revision 충돌은 409로 거절한다.
- 파일은 12MB/개, 전체 1GB 제한. 같은 사업·파일명·SHA는 중복 생성하지 않는다. 같은 이름의 수정본은 이전 파일을 유지한 별도 항목이다. 원본 파일은 정적 공개 경로에 두지 않는다.

## 예산

억 원 단위, RFP의 4개 예산연차별 국비·지방비를 기준으로 기관 배분율을 입력한다. 예산안 복사는 기존 입력을 유지하는 별도 시나리오다. 기관 유형과 소재지 미확정은 판정 대기로 표시한다. 공식 지원금 편집은 모든 시나리오의 공통 기준을 바꾼다.

기관부담 = (국비+지방비 배분액) × 부담률 / (1−부담률). 최소 현금 = 기관부담 × 현금률. 중소·초기 중견 25%/10%, 중견 30%/13%, 대기업·공기업 50%/15%, 비영리 0%를 첨부 공고의 일반 가정으로 사용한다. 예외·감면·공동연구개발비 제외는 자동 적용하지 않는다. 화면에 적용 범위와 출처를 표시한다.

지역 비율은 자부담 포함 총 연구비 기준이다. 해당 연차 배분율 100%와 유형·지역 입력이 모두 충족될 때만 50% 조건을 점검한다. 기관별 확약이나 사업자등록증까지 충족했다는 판정은 하지 않는다. 컨소시엄 편집이 저장된 예산안의 가정을 자동 변경하지 않는다.

## 공유

- `/support-share.html#<random-token>`에 읽기 전용 화면. 토큰은 32바이트 난수이며 일반 서버 로그에서 가린다.
- 서버가 토큰 해시, 만료, 해제 상태를 요청마다 검사한다. 파일 다운로드에도 동일하게 적용한다. 한국시간 12월 31일 23:59:59를 현재 연도의 기본 만료로 제안한다.
- 링크별 섹션/파일 허용 목록. 기본은 요약·요구사항·컨소시엄만. 예산·기록은 명시 선택이며 파일도 개별 선택해야 한다. 준비 체크리스트·기관 내부 메모는 절대 투영하지 않는다.
- 공유 화면에는 최신 저장본이 보인다. 링크 생성 이후 새 파일을 올려도 기존 링크에 자동 추가하지 않는다. 회사 전체 프로필, 기존 신청서 자료, 다른 사업 파일에는 접근할 수 없다.
- 링크를 발급해도 회사에 메시지나 이메일을 보내지 않는다. 화면에서 복사·열기·공유 해제를 제공한다.

## 검증

`python -m unittest test_support_projects test_support_applications test_website_intake test_static_pages -q`

인증·출처파일 보호, 변경충돌·재배포 보존, 예산/지역 분모, 미확정·과배분 처리, 공유 범위·읽기 전용·다른 사업 파일 차단, 만료·해제 및 파일 권한, 최신 저장본 표시, CSRF를 검사한다. 테스트와 브라우저 검증은 별도의 임시/로컬 DB를 사용한다.
