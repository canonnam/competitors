document.getElementById('feedbackForm').addEventListener('submit',async e=>{e.preventDefault();const status=document.getElementById('feedbackStatus');status.textContent='접수 중…';try{const response=await fetch('/api/feedback',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({category:document.getElementById('feedbackCategory').value,message:document.getElementById('feedbackMessage').value,page:'competitors'})});if(!response.ok)throw new Error();const data=await response.json();status.textContent=`개선요청 #${data.id}가 접수되었습니다.`;e.target.reset();setTimeout(closeFeedback,1200)}catch{status.textContent='접수에 실패했습니다. 잠시 후 다시 시도해주세요.'}});
