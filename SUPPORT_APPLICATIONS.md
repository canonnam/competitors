# 지원사업 신청 준비

콤파스원과 본사·연구소·요양원 사업장을 담당자 한 명이 관리한다.
`agency-news.html`의 **관심있음** 선택은 공식 공고의 원본 양식 수집만 실행한다.
`support-prep.html`에서 담당자가 사업장·팀원·기준 사업계획·참고 자료·양식을 선택하고 **초안 만들기**를 눌러야 AI 작성이 시작된다.

## 사용 순서

1. 담당자 접근키로 신청 준비 화면을 연다.
2. 기본 데이터에 회사·대표자·담당자, 사업장, 팀원, 기준 사업계획, 실적·인증 정보를 등록하고 저장한다.
3. 기존 회사소개서·사업계획서 등도 참고 자료로 등록할 수 있다. 문서에서 추출한 회사 정보는 사용자가 확인한 항목만 반영한다.
4. 지원사업에서 관심있음을 선택하면 신청 준비 목록에 연결된다. 기존 관심 사업도 자동 연결된다.
5. 각 사업의 원본을 개별 또는 ZIP으로 내려받는다. 자동 수집이 어려운 자료는 직접 추가한다.
6. 작성에 사용할 자료를 선택해 초안을 만든다. 누락 자료·작성 근거를 확인하고 항목을 수정한다.
7. 수정본 저장과 항목 다시 쓰기는 새 버전을 만든다. 원본·작성본·보완사항을 묶어 내려받을 수 있다.

## 문서 지원 범위

| 입력 | 결과 |
|---|---|
| HWP | 원본 보관. 변환 가능한 문서는 HWPX로 작성·다운로드 |
| HWPX, DOCX | 인식된 표 입력칸의 기존 구조를 유지한 작성본 |
| XLSX | 인식된 셀에 입력. 수식 셀은 보존 |
| 입력칸이 있는 PDF | 텍스트 폼 필드에 입력 |
| 일반 PDF, TXT, MD | 문서 질문에 대응하는 항목별 DOCX 초안 |
| ZIP | 지원되는 내부 문서를 분리하여 등록 |
| 동의·확약서 | 직접 확인용 원본 내용 유지. 서명·동의를 자동 실행하지 않음 |

인식 실패·스캔·특수 개체가 많은 문서는 원본을 보존하고 HWPX·DOCX·텍스트 자료의 추가 등록을 안내한다.
모든 HWP의 완전한 변환이나 모든 한글 버전의 동일한 페이지 배치를 보장하지 않는다.
최종 제출 전에 공식 원본과 내용·분량·체크·서명을 확인한다. 외부 접수 사이트에 자동 제출하지 않는다.

## 데이터와 운영

- 기존 Python/Railway 서비스에 통합. 별도 서비스나 신규 호스팅 이전 없음.
- `SUPPORT_ACCESS_KEY`: 담당자 키. 미설정 시 로그인이 차단된다. Git에 저장하지 않는다.
- `SUPPORT_DB_PATH`: 기본 `/data/support-applications.db`. Railway의 기존 영구 볼륨에 저장한다.
- `SUPPORT_WORKER_ENABLED`: 기본 `true`. 관심 사업 연결, 양식 수집, 명시적으로 요청된 초안 작업을 처리한다.
- `OPENAI_API_KEY`: 기존 서버 키 사용. 작성 화면에서 GPT-4.1 mini, GPT-4.1, GPT-5.4 mini, GPT-5.4, GPT-6 Astra를 선택한다. 기본값은 목록에 있는 `SUPPORT_OPENAI_MODEL` > `OPENAI_MODEL` > `gpt-4.1-mini` 순서이며 지원하지 않는 기본 설정값은 GPT-4.1 mini를 사용한다. 명시적으로 요청한 모델이 목록에 없으면 작업을 만들지 않고 거절한다.
- 선택한 모델을 요청 시점의 초안 스냅샷에 보관하고 작성·근거 검토·항목 재작성에 동일하게 적용한다. 서버 기본값이나 화면 선택 변경이 이전 초안의 모델을 바꾸지 않는다. 모델 선택 기능 전의 초안은 당시 확인한 GPT-4.1 mini로 표시하며 원본 저장 데이터는 변경하지 않는다. 다운로드 ZIP의 보완사항에도 사용 모델을 표시한다.
- 추론 모델은 `reasoning.effort=low`를 사용한다. 모델 선택 자체는 API를 호출하지 않는다. 회사 기본정보 추출은 서버 기본 모델을 사용한다. `/api/support/models`는 인증 후 모델 목록·기본값·공식 요금 링크를 제공한다. 표시 단가는 2026-09-12 확인한 100만 토큰당 표준 USD 단가이며 실시간 청구액이나 건당 고정 요금이 아니다. 긴 입력·캐시·추론 토큰·재시도에 따라 실제 비용이 달라진다.
- `SUPPORT_DAILY_DRAFT_LIMIT`: 기본 일 30회(UTC 기준). 진행 중 중복 생성은 거절한다.
- 파일당 12MB, 원본과 변환본 합계 1GB. 참고 파일 숨김은 이전 초안의 재현을 위해 저장 내용을 유지한다. 완전 삭제·용량 정리는 별도 운영 작업이다.
- 로그인 세션은 HttpOnly 쿠키와 서버의 해시로 관리하며 7일 뒤 만료된다. 잠금으로 해당 세션을 폐기한다. 키 변경 시 전체 기존 세션 해제는 DB의 `support_sessions`를 비우는 운영 작업으로 수행한다.
- 작성 시점의 데이터·선택 자료·공고 내용과 원본 파일의 버전을 함께 보관한다. 기본 데이터 변경이 과거 초안을 덮어쓰지 않는다.
- 공식 출처 다운로드는 공개 HTTPS 주소와 파일 형식·압축 크기를 검증한다. 문서의 매크로를 실행하지 않는다.
- AI에는 선택한 데이터와 양식의 추출 텍스트가 전달된다. Responses API의 `store=false`를 사용한다. 회사 자료·접근키는 브라우저 localStorage에 저장하지 않는다.
- AI 응답이 중단되거나 연결·일시적인 서버 오류가 발생하면 해당 요청만 한 번 재시도한다. 출력 한도에 도달했을 때는 최대 24,000토큰 안에서 한도를 조정한다. 요청 거절·입력 필터·인증·사용 한도 오류는 자동 재시도하지 않는다. 로그에는 처리 단계·응답 상태·중단 사유·토큰 수·응답 ID만 남기며 신청 내용은 기록하지 않는다.

## 검증

모델 선택 변경은 지원사업 검사 20건을 포함한 Python 검사 252건과 DOM 흐름 검사를 통과했다. 모델 5개 모두 실제 API의 구조화 응답을 확인했고, `python scripts/verify_support_forms.py --live --model gpt-5.4-mini`로 실제 HWP 양식 작성·검토·HWPX 내보내기를 검증했다. 모델 선택, 재시도, 재작성, 과거 초안의 모델 보존과 잘못된 모델의 작업 생성 차단을 검사한다.

`python -m unittest test_support_applications -q`는 접근 제어, 원본 다운로드, 등록 버전 충돌, 명시적 실행, 정확한 사실 입력, AI 근거 검토, 원본 표 보존, 초안 버전과 수식을 검사한다.

`npm install --prefix .local/dom-tests jsdom@26.1.0` 후 `node scripts/verify_support_ui.cjs`로 데이터 입력과 작성·수정·최신 버전 다운로드의 DOM 흐름을 검사한다.

`python scripts/verify_support_forms.py --live`는 설정된 AI API를 사용한다. 공개 인천 AX 공고의 실제 HWP 양식을 임시 DB에 수집하고 명시적인 가상 회사 정보로 작성한다. 운영 데이터는 수정하지 않는다. HWPX 재분석, 회사명·대표자 위치, 원본 구조, 동의서 보존을 검증한다. 결과는 Git에서 제외된 `.local/support-verification`에 기록한다.

2026-09-12 지원사업 회귀 검사 12건, 뉴스·관심 처리 JavaScript 검사 8건, 등록·작성·수정·다운로드 DOM 검사를 통과했다. 실제 HWPX 문서는 독립 렌더러로 출력 배치도 확인했다. 최신 운영 브랜치의 변경을 함께 반영해 기존 기능의 검사를 수행했다.
