# 지점별 청구 점검

홈 카드와 `/claim-check.html`은 `/api/claim-check`에 저장된 공단 조회 결과를 표시한다.
기존 Railway 서비스와 `/data` 볼륨을 사용한다. 현재 PC의 하드디스크 인증서와
AnySign 웹 팝업으로 두 지점 로그인 및 청구서 조회를 검증했다.
Linux 웹서버에서는 PC 인증서에 접근할 수 없으므로 공단 조회는 연결된 PC 브라우저에서 실행한다.
페이지의 새로고침은 저장 결과 조회이며 공단 로그인 실행 버튼이 아니다.
정기 점검은 이 작업의 heartbeat 예약 `automation`으로 매월 6~10일 오전 11시(한국시간)에 실행한다.
대상 월의 두 지점 모두 접수 완료이고 인건비 비율도 확인했으면 다음 달 6일로 예약을 넘긴다.
청구 완료 후 인건비만 미확인이면 청구 상태를 다시 조회하지 않고 인건비만 보충한다.

## 판정

- 한국시간 현재 달의 직전 급여제공월만 조회한다. 9월에는 8월을 조회하고 마감은 9월 10일이다.
- 필요한 조합은 `노인요양시설(개정법) / 일반`, `노인요양시설(개정법) / 의료`, `장기근속장려금 / 일반`이다.
- 세 조합이 모두 존재하고 해당 청구의 처리상태가 모두 정확히 `심사`일 때만 접수 완료다.
- 실제 조회에서 항목 누락이나 다른 처리상태가 확인되면 점검이다. 중복 일반 청구가 의료 청구를 대신하지 못한다.
- 재조회 실패는 같은 급여제공월의 마지막 성공 조회 결과와 확인 시각을 보존하고 별도로 표시한다.
  이번 급여제공월의 성공 조회 기록이 전혀 없을 때만 조회 실패를 점검으로 표시한다.
- 다른 급여종류는 이 판정에 포함하지 않는다. 월이 바뀌면 이전 월의 내역과 완료 표시를 숨긴다.
- 결과에는 실제 마지막 조회 시각을 표시한다. 조회 실패를 미청구로 단정하지 않는다.
- 인건비는 청구 급여제공월보다 한 달 앞선 급여제공월로 조회한다. 9월 점검 시 청구는 8월,
  인건비는 7월이다. 연도는 인건비 조회월의 연도로 적용한다.
- 사용자가 지정한 2026년 운영 기준은 62.5% **초과**일 때 안정, 이하이면 점검 필요다.
  법정 기준의 자체 검증 결과로 표현하지 않는다. 다른 연도의 기준을 임의로 같게 적용하지 않는다.
- 인건비 판정은 청구 접수 상태와 별개다. 비율 조회 실패는 기존 청구 완료를 바꾸지 않는다.
  같은 조회월의 비율이 이미 있으면 비율과 확인 시각도 보존한다.

## 인건비 비율 조회와 게시

1. 해당 지점 로그인과 기관 기호를 확인한 뒤 **요양자원 → 종사자인건비 지출내역 신고**로 이동한다.
2. 급여제공월을 청구 대상월의 직전 월로 설정하고 조회한다. 예: 청구 2026-08 → 인건비 2026-07.
3. 화면의 **16번 연간비율(해당 연도)** 값을 그대로 읽는다. 월간비율이나 금액을 대신 쓰거나
   직접 재계산하지 않는다. 신고·저장·제출·삭제 버튼은 누르지 않는다.
4. 조회월·연도·기관과 항목 번호를 확인한 경우에만 `querySucceeded: true`로 기록한다.
   `annualRatio`는 % 기호를 제외한 숫자 문자열이며 화면의 소수 자릿수를 보존한다.
   값을 읽지 못하면 `querySucceeded: false`, `annualRatio: null`과 실제 시도 시각을 기록한다.
   직원별 급여·이름·주민번호 등은 수집하지 않는다.
5. 기존 청구 결과가 저장되어 있다면 `.local/labor-result.json`에 아래 구조로 두 지점을 기록하고
   `python scripts/publish_claim_check.py .local/labor-result.json --labor-only --publish`로 게시한다.
   이 경로는 운영 청구 내역과 확인 시각을 변경하지 않고 인건비 부분만 갱신한다.
   새달의 청구 결과가 아직 없으면 기존 전체 청구 게시 객체에 `laborCost`를 함께 넣는다.
6. 운영 API에서 각 지점의 `laborCost` 값·조회월·연도·확인 시각까지 대조한다.
   `allAccepted`는 청구만, `allChecksComplete`는 두 지점 청구 완료와 인건비 조회 완료를 뜻한다.
   운영 기준 미달 여부는 조회 완료 여부와 별개이며 완료된 월을 계속 재조회할 이유로 쓰지 않는다.

```json
{
  "benefitMonth": "YYYY-MM (청구월)",
  "branches": [{
    "id": "anyang",
    "institutionNumber": "14117000625",
    "laborCost": {
      "benefitMonth": "YYYY-MM (청구월의 직전 월)",
      "year": 2026,
      "checkedAt": "실제 조회 시각 ISO 8601 +09:00",
      "querySucceeded": false,
      "annualRatio": null
    }
  }]
}
```

2026-09-20 재시도에서 Codex 내장 브라우저로 두 지점의 하드디스크 공동인증서 로그인과
위 메뉴의 실제 조회를 검증했다. 조회 화면의 `급여제공연월`과 `급여유형`을 확인한 뒤
**신고내역 조회**를 누르고 로딩이 끝나면 **⑯ 연간비율(2026년도)** 옆의 값을 읽는다.
연간비율 입력란의 화면 DOM ID는 `div_psexpMmRat.form.edt_yyyyPayRat:input`으로 끝난다.
최초 화면에는 빈 값과 임시 연도가 표시될 수 있으므로 조회 완료 후의 연도와 값을 확인한다.

로그인 후 자동으로 열린 업무포털 팝업에서 클릭과 화면 캡처가 시간 초과되면,
브라우저 탭 목록에서 관찰한 해당 팝업의 URL을 같은 내장 브라우저의 일반 탭으로 열면 된다.
일반 탭에서 안내 팝업의 **닫기**를 누른 뒤 조회 메뉴로 이동하고 기관 기호를 다시 확인한다.
세션이 포함된 URL은 파일이나 예약 문구에 저장하지 않는다. 사용한 팝업과 일반 탭은
조회 후 닫고, 지점을 전환할 때 원래 로그인 페이지에서 로그아웃한다.

## PC에서 조회하고 결과 갱신

1. 현재 작업의 사용자 메시지에 제공된 기관별 인증서 이름과 **최신 정정된** 암호를 사용한다.
   암호·인증서·개인키·로그인 세션을 저장하거나 웹서버로 보내지 않는다.
2. 공단 로그인 페이지에서 장기요양 관련기관 → 장기요양기관을 선택한다.
   안양 `14117000625`, 인천 `12817700685`를 각각 입력하고 법인인증서 로그인을 누른다.
3. 하드디스크를 선택하고 안양은 `더비다요양원`, 인천은 `매화요양원2` 인증서를 고른다.
   가상키보드는 매번 배열이 달라지므로 최신 스크린샷으로 각 키를 확인한다.
   Shift 전환 이후 화면이 바뀐 것을 확인하고 특수문자를 입력한다.
   입력은 마스킹 길이로 확인한다. 암호 오류 시 반복 제출하지 말고 원인을 확인한다.
4. 로그인한 페이지의 기관 기호를 검증한다. 자동으로 열린 업무포털에서
   급여비용청구 → 조회 및 출력 → 청구서 목록 조회로 이동한다.
5. 조회조건 `급여제공월`, 처리상태 `전체`를 확인한다. 시작월과 종료월을 모두 대상 월로 설정한다.
   Nexacro 날짜 필드는 클릭 → 전체 선택 → `YYYYMM` 입력 → Tab으로 확정하고 표시값을 확인한다.
6. 조회 결과 전체에서 필수 세 조합, 각 청구일 및 처리상태를 읽는다.
   청구 제출·취소·행삭제·자진신고는 누르지 않는다. 기관 기호와 대상 월이 다르거나
   결과를 끝까지 읽지 못하면 `querySucceeded: false`와 빈 `claims`로 기록한다.
7. 아래 형식으로 `.local/claim-result.json`을 작성한다. 두 지점 모두 실제 조회 시각을 사용한다.
   금액, 접수번호, 수급자 이름·인원 등 불필요한 데이터는 포함하지 않는다.
   조회 실패도 실제 시도 시각과 빈 목록으로 게시하면 서버가 기존 성공 결과를 보존하고
   `lastQueryFailureAt`만 갱신한다. 과거 성공 시각을 현재 시각으로 바꾸지 않는다.
8. `python scripts/publish_claim_check.py .local/claim-result.json`으로 형식을 검증한 뒤
   `python scripts/publish_claim_check.py .local/claim-result.json --publish`로 반영한다.
   이 명령은 기존 Railway CLI 인증으로 해당 서비스의 `/data/claim-check.json`만 원자적으로 갱신하고
   실제 API의 월과 성공 조회 또는 실패 시각을 대조한다. API는 읽기 전용이며 별도 쓰기 키를 만들지 않는다.

결과 구조 (`branches`에는 아래 객체를 안양·인천 각각 넣고, `claims`에는 실제 조회된 모든 필수 조합의 청구를 넣는다):

```json
{
  "benefitMonth": "YYYY-MM",
  "branches": [{
    "id": "anyang",
    "institutionNumber": "14117000625",
    "checkedAt": "실제 ISO 8601 조회 시각 +09:00",
    "querySucceeded": true,
    "claims": [{
      "benefitMonth": "YYYY-MM",
      "benefitType": "노인요양시설(개정법)",
      "recipientType": "일반",
      "claimType": "원청구",
      "submittedOn": "YYYY-MM-DD",
      "processingStatus": "심사"
    }]
  }]
}
```

필요 시 `CLAIM_CHECK_PATH`로 저장 위치를 지정할 수 있다. 운영 기본값은 `/data/claim-check.json`이다.
실제 조회 결과는 Git에 커밋하지 않고 `.local/`과 운영 볼륨에만 보관한다.

검증: `python -m unittest test_claim_check test_static_pages test_site_identity` 및
`node --check assets/claim-check.js`, `node test_claim_check.cjs`, `python -m unittest test_ui_consistency`.
