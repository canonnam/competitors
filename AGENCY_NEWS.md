# 건보공단·복지부 뉴스 운영

홈 세 번째 카드는 `/agency-news.html`로 연결한다. 서버가 공개 게시판 9개를 매일 한국시간 오전 9시 10분에 확인하고, 요양원 운영·장기요양·돌봄 사업 관련 글의 제목, 게시일, 분야, 원문 링크를 저장한다. 방문 여부와 무관하게 실행하며 별도 API 키는 필요 없다.

2026-09-09부터 기업마당 지원사업을 더해 총 10개 출처를 확인한다. 회사 조건에 따른 추천과 새 공고 확인 기능은 [BUSINESS_SUPPORT.md](BUSINESS_SUPPORT.md)를 참고한다. 아래의 최초 수집 범위와 파싱 설명은 기존 공공기관 게시판 9개에 해당한다.

## 수집 범위

정확한 원본 URL은 `data/agency_news_sources.json`에서 관리한다.

| 기관 | 게시판 | 식별자 |
| --- | --- | --- |
| 국민건강보험공단 | 평가 공지사항 | B0152 |
| 국민건강보험공단 | 평가 매뉴얼 | B0153 |
| 국민건강보험공단 | 지역본부소식 | B0045 |
| 국민건강보험공단 | 보도자료 | B0010 |
| 국민건강보험공단 | 서식자료실 | B0017 |
| 국민건강보험공단 | 법령자료실 | B0018 |
| 국민건강보험공단 | 통계자료실 | B0019 |
| 보건복지부 | 보도자료 | 0027 |
| 보건복지부 | 공지사항 | 0003 |

공단은 사용자가 지정한 페이지의 공개 게시판 iframe을 읽는다. 평가·서식·법령·통계 게시판은 장기요양 전용 게시판이라는 맥락으로 선별하고, 지역본부소식·보도자료는 제목에서 관련성을 확인한다. 복지부는 전체 제목과 본문을 확인하되 본문이나 첨부파일 전체를 복제하지 않는다. 선별 규칙은 `agency_news.py`의 `relevance`에 있으며, 장기요양·요양원·통합돌봄·요양보호사·돌봄 기술 등을 포함하고 관련 없는 아동 정책·채용·포상 등을 제외한다. 규칙 기반 분류이므로 표현이 바뀐 글은 누락되거나 폭넓은 정책 글이 포함될 수 있다.

최초 수집은 게시판별 최근 2페이지의 90일 이내 글과 고정 공지로 시작한다. 과거 90일 전체를 소급 수집하는 기능은 아니다. 이후에는 이미 확인한 일반 게시글까지 페이지를 따라가며 신규 글을 모은다. 고정 공지를 만났다는 이유로 탐색을 중단하지 않으며, 중단 기간이 있어도 40페이지까지 탐색한다. 한도 도달 시 성공으로 기록하지 않는다. 게시판·글 ID로 중복을 방지하고, 확인한 글의 제목이나 게시일 변경도 반영한다. 수집한 글은 기간이 지나도 삭제하지 않는다.

## 실행과 저장

- `AGENCY_NEWS_SYNC_ENABLED=true` (기본값): 앱 시작 시 데몬 스케줄러를 실행한다.
- `AGENCY_NEWS_DB_PATH=/data/agency-news.db` (기본값): 기존 Railway `/data` 볼륨에 게시글, 확인 이력, 게시판별 성공/실패 상태를 보관한다.
- 처음 실행하거나 예정 시간을 놓쳤으면 즉시 수집한다. 정상 확인한 게시판은 다음 오전 9시 10분까지 다시 요청하지 않는다.
- 특정 게시판 요청·파싱이 실패하면 해당 게시판의 직전 글과 확인 이력을 유지한다. 다른 게시판 수집은 계속하며 실패한 게시판만 30분 후 재시도한다.
- 목록의 수집 상태와 홈 배지는 실제 성공 시각을 사용한다. 일부 게시판 실패를 전체 성공으로 표시하지 않는다.
- 운영은 항상 실행 중인 단일 앱 인스턴스를 기준으로 한다. API를 열거나 화면을 새로고침하는 행위는 외부 수집을 시작하지 않는다.

즉시 수집은 앱과 같은 환경에서 `python agency_news.py --sync`, 상태 확인은 `python agency_news.py`로 실행한다. 실패한 게시판이 있으면 수동 수집 명령은 종료 코드 1을 반환한다.

`GET /api/agency-news`는 게시글과 상태를 반환한다. `GET /api/agency-news?summary=1`은 홈용 요약이며 게시글 목록을 제외한다. 두 응답 모두 캐시하지 않는다. 브라우저는 최초 방문, 탭 복귀, 5분 간격으로 저장된 결과를 다시 확인한다.

## 확인

`python -m unittest test_agency_news`와 `node --test test_agency_news.cjs`로 공식 게시판 HTML 파싱, 관련성 분류, 페이지 탐색, 재시작 중복 방지, 오류 시 기존 자료 유지, 일정 경계, API, 홈 순서, 링크 제한과 배지를 검증한다.

2026-09-08 로컬 실수집에서 지정 게시판 9개가 모두 성공했고 관련 글 54건이 저장됐다. 게시판별 필터와 검색을 제공하며, 원문 링크에서 첨부파일과 실제 적용 범위를 확인할 수 있다.
